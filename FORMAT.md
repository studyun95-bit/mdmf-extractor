# 확인한 MDMF 내부 구조

이 문서는 제공된 샘플과 Windows 뷰어의 처리 루틴을 대조해 확인한 **`MDMFILEFXC` / 버전 `11` / 헤더 2,227바이트** 구성을 설명합니다. 공개된 공식 규격이 아니며 모든 MDMF 변형에 적용된다고 보장하지 않습니다. 실제 공문, 문서 정보, 파일별 키는 이 문서와 저장소에 포함하지 않습니다.

MDMF는 확장자만 ZIP으로 바꾸어 풀 수 있는 파일이 아닙니다. 확인한 구성은 **암호화된 PDF 본문과 별도 첨부파일을 담는 컨테이너**입니다. 첨부파일은 먼저 복호화하고 압축을 풀어 원본 바이트를 복원합니다. HWP·HWPX를 PDF로 변환하는 과정은 없습니다.

## 표기와 암호화

오프셋은 0부터 시작하는 바이트 위치입니다. ASCII 십진수 길이 필드는 고정 폭이고, `u32be`는 부호 없는 4바이트 빅 엔디언 정수입니다.

복호화 함수 `D(data, key)`는 다음과 같습니다.

- AES-128-CBC를 사용하며 각 **256바이트 조각마다 0으로 채운 16바이트 IV로 초기화**합니다.
- 각 조각에서 완전한 16바이트 블록만 복호화합니다. 마지막에 남은 1~15바이트는 변경하지 않습니다.
- PKCS#7 패딩을 사용하거나 제거하지 않습니다. 암호화 전후 길이는 같습니다.
- 헤더에는 뷰어에서 확인한 고정 키 `dONGHwaMAnSeMfan`을 사용합니다. 본문과 첨부파일에 쓰는 키는 복호화한 헤더에서 읽습니다.

## 본문까지의 배치

| 원본 오프셋 | 길이 | 내용 |
| --- | ---: | --- |
| `0` | 10 | ASCII `MDMFILEFXC` |
| `10` | 10 | ASCII 십진수 헤더 길이: `0000002227` |
| `20` | 2 | ASCII 버전: `11` |
| `22` | 2,227 | 암호화된 헤더 |
| `2249` | 10 | ASCII 십진수 메타데이터 길이 `M` |
| `2259` | `M` | 메타데이터 |
| `2259 + M` | `R` | 별도 레코드 영역 |
| `2259 + M + R` | `P` | 암호화된 PDF |
| `2259 + M + R + P` | 가변 | 첨부파일 영역 |

복호화한 헤더에서 사용하는 필드는 다음과 같습니다. 표에 없는 필드의 의미는 이 추출기에서 해석하지 않습니다.

| 헤더 내부 오프셋 | 길이 | 내용 |
| --- | ---: | --- |
| `0` | 10 | ASCII 십진수 PDF 길이 `P` |
| `10` | 10 | 추가 영역 길이: 현재 지원 구성에서는 `0` |
| `404` | 10 | ASCII 십진수 별도 레코드 길이 `R`: 현재 지원 구성에서는 `20` |
| `1013` | 16 | 첨부 목록용 키 `K1` |
| `1029` | 16 | PDF용 키 `K2` |
| `1045` | 16 | 첨부 내용용 키 `K3` |

PDF는 `D(암호화된 PDF, K2)`로 복원합니다. 추출기는 `%PDF-` 시작, `%%EOF` 끝 및 PDFKit으로 읽을 수 있는 페이지를 확인합니다. 이는 문서 발급자의 전자서명이나 진위 검증을 대신하지 않습니다.

## 첨부파일 영역

PDF 바로 다음에 ASCII **`MATTACHDAT` 10바이트**가 옵니다. 별도의 NUL 구분자는 없습니다. 이후 배치는 다음과 같습니다.

| 마커 직후 기준 오프셋 | 길이 | 내용 |
| --- | ---: | --- |
| `0` | 4 | `u32be` 총길이 `T`: 이 8바이트 길이 필드를 제외한 목록과 내용의 합 |
| `4` | 4 | `u32be` 암호화된 목록 길이 `L` |
| `8` | `L` | 암호화된 목록: `D(목록, K1)`로 복호화 |
| `8 + L` | `T - L` | 첨부 내용 영역 |

복호화한 목록은 `u32be 정보 길이`, 해당 길이의 문서 정보, `u32be 첨부 개수 N`, **268바이트 레코드 `N + 1`개** 순서입니다. 첫 레코드는 PDF를 가리키며 첨부 개수에 포함되지 않습니다. 이어지는 `N`개가 실제 첨부파일입니다. 문서 정보는 추출에 필요하지 않으므로 해석하지 않습니다.

각 레코드의 구조는 다음과 같습니다.

| 레코드 내부 오프셋 | 길이 | 내용 |
| --- | ---: | --- |
| `0` | 256 | CP949 파일명, 첫 NUL까지 읽음 |
| `256` | 4 | `u32be` 원본 파일 길이 `U` |
| `260` | 4 | `u32be` 저장된 내용 길이 `C` |
| `264` | 4 | `u32be` 첨부 내용 영역 시작을 기준으로 한 오프셋 `O` |

첨부별로 내용 영역의 `[O, O + C)`를 읽고 **각 파일의 시작에서** `D(내용, K3)`를 적용합니다. 파일명은 표시와 저장에만 사용하며 경로로 신뢰하지 않습니다. 예를 들어 `붙임1.hwp`와 `붙임2.hwpx`는 각자 독립된 파일로 복원됩니다.

## 첨부 내용의 압축 프레이밍

복호화한 각 첨부 내용은 다음 순서입니다.

```text
u32be 원본 파일 길이 U
반복:
    u32be zlib 스트림 길이 Z
    Z바이트의 독립된 zlib 스트림
```

확인한 뷰어는 원본을 최대 **30,000바이트씩** 나누고, 조각 뒤에 NUL 1바이트를 덧붙인 뒤 각각 zlib으로 압축합니다. 따라서 정상적인 전체 조각을 풀면 30,001바이트이며 마지막 조각은 더 짧을 수 있습니다. 각 스트림을 완전히 해제한 뒤 **추가된 마지막 NUL 한 바이트만** 제거하고 순서대로 연결합니다. 원본 자체에 포함된 NUL은 보존해야 합니다.

복원 결과 길이는 내용 첫 필드와 목록 레코드의 `U`에 모두 일치해야 합니다. 스트림 경계, 압축 해제 크기, 목록과 내용 범위도 확인해야 하며, 일치하지 않는 값을 추측해서 보정하면 안 됩니다. 이 방식으로 HWP의 원본 컨테이너와 HWPX의 ZIP 바이트를 복원할 수 있습니다.

## 확인 범위

이 구조는 첨부가 없는 PDF 공문과 HWP·HWPX 첨부가 함께 있는 공문에서 확인했습니다. 다른 헤더 길이, 다른 버전, 사용자 암호가 필요한 형식, 다른 압축 프레이밍은 별도 검증이 필요합니다. 추출기는 원본 콘텐츠를 실행하지 않으며, 전용 뷰어의 전자서명 검증 기능을 구현하지 않습니다.
